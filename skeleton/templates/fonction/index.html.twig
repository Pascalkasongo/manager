{{ include('content/navbar.html.twig')}}
{{ include('content/slidebar.html.twig')}}
{% block title %}Fonction index{% endblock %}

{% block body %}
<div class="pcoded-content">
    <div class="container mt-4">
        <h1>Fonction(s)</h1>

        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nom_fonction</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for fonction in fonctions %}
                <tr>
                    <td>{{ fonction.id }}</td>
                    <td>{{ fonction.nomFonction }}</td>
                    <td>
                        <a href="{{ path('app_fonction_show', {'id': fonction.id}) }}">show</a>
                        <a href="{{ path('app_fonction_edit', {'id': fonction.id}) }}">edit</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
  
    <a  class="btn btn-primary"href="{{ path('app_fonction_new') }}">Créer</a>
</div>
</div>
    {{ include('content/data.html.twig')}}
{% endblock %}
